[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "python-algebraic-inverted-index"
version = "0.3.0"
description = "Generic set algebra over inverted field/value indexes"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["setindex"]
